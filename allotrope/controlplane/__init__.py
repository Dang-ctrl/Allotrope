"""gRPC control plane: state/telemetry distribution and liveness.

See `allotrope.proto` for the wire schema and its own scope notes, and
`docs/control-plane.md` for what this covers and what it deliberately
doesn't.
"""
