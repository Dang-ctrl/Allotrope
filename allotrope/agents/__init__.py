"""The learned controllers: DQN for commitment, SDDPG for dispatch.

See `hybrid.HybridAgent` for the combined policy, `train.train` for the
training loop, and `checkpoint` for saving and loading a trained agent.
"""
