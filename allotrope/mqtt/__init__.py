"""The telemetry and federated-update link (MQTT), separate from actuation (gRPC).

See `publisher.TelemetryPublisher`, `subscriber.TelemetrySubscriber`, and
`codec` for the defensive JSON encoding used on both sides.
"""
