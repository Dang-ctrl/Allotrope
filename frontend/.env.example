VITE_API_BASE_URL=http://localhost:8000

# Must match the backend's ALLOTROPE_API_KEY (or the key it logs at startup
# if that's unset) -- required for the start/stop/reset/step buttons to
# work. See allotrope/api/app.py's module docstring.
VITE_API_KEY=
