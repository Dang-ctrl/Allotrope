.venv/
__pycache__/
*.py[cod]
.pytest_cache/
checkpoints/
*.pt
.git/
tests/
docs/
*.md
# the web frontend is served by its own toolchain, never from this image --
# and node_modules would otherwise be walked as part of the build context
webapp/
